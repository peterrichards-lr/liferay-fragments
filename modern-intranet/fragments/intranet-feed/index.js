function initIntranetFeed() {
  const feedContainer = fragmentElement.querySelector(".intranet-feed-items");
  if (!feedContainer) return;

  // Placeholder for future AJAX logic to fetch posts
  console.log("Intranet Feed initialized");
}

initIntranetFeed();
