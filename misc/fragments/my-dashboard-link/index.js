const initMyDashboardLink = () => {
  if (layoutMode === "view") {
    const link = fragmentElement.querySelector("a");
    if (link) {
      // Dashboard link logic
    }
  }
};

initMyDashboardLink();
