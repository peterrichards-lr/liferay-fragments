# Profile Detail (Deprecated)

> **Warning**: This fragment is now **deprecated**. It relies on the legacy `serviceLocator` pattern which is restricted in many Liferay environments for security reasons. Users are encouraged to use modern **Data Providers** or **Headless APIs** to fetch user profile details.

The **Profile Detail** fragment provides a comprehensive view of a user's profile attributes and contact information.
