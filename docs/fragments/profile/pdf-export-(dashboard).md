# PDF Export (Dashboard) (Deprecated)

> **Warning**: This fragment is now **deprecated**. It relies on specific page
> structures and legacy patterns. Modern alternatives like the **Meta-Object
> Record View** (which includes high-fidelity PDF export) should be used
> instead.

The **PDF Export (Dashboard)** fragment is a specialized version of the PDF
export tool designed for dashboard environments.
